[project]
name = "subconv"
version = "2.0.0"
requires-python = ">=3.13"
dependencies = [
    "fastapi==0.111.0",
    "httpx==0.27.0",
    "pyyaml>=6.0",
    "uvicorn==0.30.1",
    "pydantic-settings>=2.8",
]

[dependency-groups]
build = [
    "nuitka[onefile]",
]

[tool.uv]
package = false
